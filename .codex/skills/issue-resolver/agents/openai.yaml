interface:
  display_name: "issue-resolver"
  short_description: "不具合報告から修正完了までを一貫して進める"
  default_prompt: |
    入力コマンドに応じて以下を実行してください。

    `analyze-issue <URL|テキスト>`:
    1) 不具合報告を分析し、再現手順と原因仮説（最大3件）を整理する。
    2) URL調査が必要な場合は、`require_escalated` を使った承認付きの制限外実行で取得する。
    3) evo/DB確認は必ずDockerコンテナ内で実行する。
    4) 原因判明後は、修正前に調査報告（原因・影響範囲・修正方針案）を提示する。

    `reproduce`:
    1) 最小再現手順を作る。
    2) ログ・DB・キャッシュ状態を確認して再現条件を固定する。
    3) 再現後の観測結果を要約する。

    `create-branch`:
    1) Issue 番号と症状からブランチ名を提案する。
    2) ベースブランチを確認してから分岐する。

    `draft-plan`:
    1) 問題が分かりにくい場合や実装が複雑な場合にのみ ExecPlan を作成する。
    2) `analyze-issue` の結果を目的・制約・検証方法つきで計画へ反映する。

    `implement-fix`:
    1) 調査報告への承認後にのみ着手する。
    2) 軽微な修正はExecPlan作成を省略して進めてよい。
    3) 問題が分かりにくい場合や修正が複雑な場合は先にExecPlanを作成する。
    4) エラー隠蔽を目的とした修正は行わず、原因データの発生源を修正する。

    `archive`:
    1) 修正内容を再現条件とセットで要約する。
    2) Conventional Commits 形式のコミットメッセージ案を作る。

    `pull-request`:
    1) PR タイトルと本文を `What / Why / How / Test` の順で下書きする。
    2) 破壊的変更、互換性影響、運用手順の有無を明記する。

    共通ルール:
    1) 不具合報告を分析し、再現手順と原因仮説（最大3件）を整理する。
    2) URL調査が必要な場合は、`require_escalated` を使った承認付きの制限外実行で取得する。
    3) evo/DB確認は必ずDockerコンテナ内で実行する。
    4) 原因判明後は、修正前に調査報告（原因・影響範囲・修正方針案）を提示する。
    5) 修正着手は調査報告への承認後に限定する。勝手に修正を実施しないこと。
    6) 軽微な修正はExecPlan作成を省略して進めてよい。
    7) 問題が分かりにくい場合や修正が複雑な場合は先にExecPlanを作成する。
    8) エラー隠蔽を目的とした修正は行わず、原因データの発生源を修正する。
       例: `strpos()` へ渡る `null` を空文字へ変換して通すだけの対処は禁止。
