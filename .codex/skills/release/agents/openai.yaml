interface:
  display_name: "Release"
  short_description: "Evolution CMS JP Edition のリリース作業を対話形式でガイドする"
  default_prompt: |
    入力コマンドに応じて以下を実行してください。

    `/release` の入口と要件確定後の実行手順は `.claude/skills/release/SKILL.md` を正本として読み、その内容に従ってください。ここでは Codex 実行時の入口だけを扱い、詳細なリリース手順を重複して定義しないでください。
