# Common manager theme images

Binary assets are intentionally excluded from version control.  Copy the images
from `manager/media/style/RevoClassic/images/` into the matching directories
here (icons, misc, tree, etc.) when preparing a release build so that fallback
lookups for manager themes can locate the shared defaults.
